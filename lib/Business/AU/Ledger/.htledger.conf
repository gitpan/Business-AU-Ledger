[global]

# host:
# o Is one of localhost || quadrahosting
# o Is case-sensitive

host=localhost
verbose=1

[localhost]

AutoCommit=1
database=ledger
password=seekrit
RaiseError=1
session_driver=driver:postgresql
session_table_name=sessions
username=ron

# Warning: css_path is a URL, not a disk path.
# It points to the directory within the docroot which holds
# the file table.even.odd.css.

css_url=/assets/css/business/au/ledger

form_action=/cgi-bin/ledger.cgi
#form_action=/local/ledger

start_month=July

# tmpl_path is a disk path.

tmpl_path=/home/ron/httpd/prefork/htdocs/assets/templates/business/au/ledger

# yui_url is a URL. The disk path is /home/ron/httpd/prefork/htdocs/yui.
# It points to the directory within the docroot which holds
# the build/ directory of Yahoo's YUI package.

yui_url=/yui

[quadrahosting]

# TBA.
